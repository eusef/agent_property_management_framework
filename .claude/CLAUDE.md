# PM_FRAMEWORK — Claude Code Entry Point

This project uses `AGENT_CONTEXT.md` as its primary AI entry point.

**Read `AGENT_CONTEXT.md` in the project root for full context.**

It contains the project overview, file map, workflows, approval thresholds, and all instructions needed to assist with property management tasks.
