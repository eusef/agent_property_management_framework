[pytest]
testpaths = tests
markers =
    unit: Unit tests for individual modules
    integration: Integration tests for API routes
    acceptance: Acceptance tests mapped to PRD acceptance criteria
    slow: Tests that may take more than 1 second
asyncio_mode = auto
