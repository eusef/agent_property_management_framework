# PM_FRAMEWORK

**AI-assisted property management for independent landlords.**

Stop juggling spreadsheets, email threads, and sticky notes. PM_FRAMEWORK gives you a structured, file-based system that works with any AI assistant to help you manage rental properties like a pro — without the cost of a property management company.

## What is this?

A ready-to-use directory structure + web dashboard for managing rental properties. Everything lives in plain-text files (Markdown, CSV) that you own and control. Point your AI tool at it, and it becomes your property management assistant.

- **Track** rent, expenses, maintenance, and compliance
- **Generate** tenant communications from battle-tested templates
- **Monitor** deadlines, lease renewals, and required inspections
- **Stay compliant** with jurisdiction-specific checklists and calendars

## Quick Start

### Option A: Double-click (recommended)

1. **macOS** — Double-click `START.command`
2. **Windows** — Double-click `START.bat`
3. **Linux** — Run `./START.sh`

The launcher handles everything: checks for Python, creates a virtual environment, installs dependencies, and opens the browser. On first launch, a setup wizard walks you through configuring your properties.

### Option B: Command line

```bash
# 1. Clone the repo
git clone https://github.com/YOUR_USERNAME/PM_FRAMEWORK.git
cd PM_FRAMEWORK

# 2. Launch (auto-installs dependencies, opens browser)
# macOS:
./START.command
# Linux:
./START.sh
```

If the server is already running, the launcher just opens your browser to it.

### Option C: Manual setup (advanced)

```bash
cd webapp
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python -m app
# → Opens http://localhost:8000
```

## First-Run Setup Wizard

On first launch, you'll be guided through an 8-step browser-based setup:

1. **Welcome** — Overview and getting started
2. **Owner Profile** — Your name, email, and phone
3. **Jurisdiction** — Select your state for compliance tracking
4. **Add Property** — Enter your first rental property
5. **Properties List** — Review, edit, or add more properties
6. **AI Setup** — Choose your AI assistant tool
7. **Review** — Confirm everything before setup
8. **Complete** — Property directories and config files are created

Your progress is saved at each step — close the browser and come back anytime. All configuration is stored in a local `config.json` file.

## Folder Structure

```
PM_FRAMEWORK/
├── START.command / .bat / .sh # Double-click launchers (macOS/Win/Linux)
├── config.json               # Generated by setup wizard
├── AGENTS.md                 # Pointer to .pm_agent/ for AI assistants
├── DASHBOARD.md              # Portfolio overview
├── TASKS.md                  # Active task tracker
├── DECISIONS.md              # Decision log
│
├── .pm_agent/                # AI agent config (hidden from web UI)
│   ├── AGENT_CONTEXT.md      # Full project context and workflows
│   └── AGENT_SETUP.md        # Working instructions and CSV schemas
│
├── properties/
│   └── property-template/    # Blank template — copy for new properties
│       ├── README.md         # Property card template
│       ├── maintenance-forecast.md
│       └── financials/       # income.csv, expenses.csv, deposits.csv
│
├── templates/
│   ├── comms/                # Tenant communication templates (7)
│   ├── checklists/           # Inspection & review checklists (5)
│   └── forms/                # Links to state-specific forms
│
├── compliance/               # Jurisdiction-agnostic legal templates (9)
├── examples/oregon/          # Oregon-specific compliance package
├── docs/                     # Guides: getting-started, how-it-works, FAQ
├── vendors/                  # Vendor directory & performance tracking
├── financials/               # Portfolio-level financials & tax docs
├── continuity/               # Business continuity & succession planning
│
├── webapp/                   # FastAPI + HTMX web dashboard
│   ├── app/                  # Application source
│   │   ├── routers/          # API routes (dashboard, files, onboarding, settings)
│   │   ├── services/         # Business logic (config, onboarding, dashboard)
│   │   ├── templates/        # Jinja2 HTML templates
│   │   └── static/           # CSS, JS, icons, PWA assets
│   ├── tests/                # Unit, integration, acceptance tests
│   └── requirements.txt
│
├── setup.sh                  # CLI setup (alternative to browser wizard)
└── .claude/CLAUDE.md         # Claude Code pointer → .pm_agent/
```

## Web Dashboard

The included web dashboard provides:

- **Browser-based setup wizard** — No terminal required for first-time configuration
- **File browser** with Markdown rendering and side-by-side editing
- **Property cards** with key metrics pulled from property READMEs
- **Task tracking** with interactive checkbox support
- **Full-text search** across all documents
- **Settings page** — Edit owner info, properties, and jurisdiction anytime
- **Installable as a desktop app** (PWA) — Works like a native application
- **Offline support** — Shows a friendly message when the server isn't running

## Works With Any AI Tool

PM_FRAMEWORK is **tool-agnostic**. The `.pm_agent/AGENT_CONTEXT.md` file serves as the universal entry point:

| Tool | How to use |
|------|-----------|
| **Claude Code** | Automatically reads `.claude/CLAUDE.md` → `.pm_agent/AGENT_CONTEXT.md` |
| **ChatGPT / GPT-4** | Paste or upload `.pm_agent/AGENT_CONTEXT.md` as system context |
| **Cursor** | Add `.pm_agent/AGENT_CONTEXT.md` to your project context |
| **Any LLM** | Point it at `.pm_agent/AGENT_CONTEXT.md` for full project awareness |

You select your AI tool during setup. Change it anytime in Settings.

## Getting Started

Copy `properties/property-template/` to create your first property. See `docs/adding-a-property.md` for a step-by-step guide.

## Settings

After setup, click the gear icon in the dashboard header to:
- Edit your owner profile
- Add, edit, or remove properties
- Change your state/jurisdiction
- Re-run the setup wizard

## FAQ

**Q: Do I need to know how to code?**
No. Double-click the START file, complete the browser wizard, and you're ready. Everything is plain Markdown and CSV files.

**Q: Is my data stored in the cloud?**
No. Everything stays on your local machine in plain-text files you control. No accounts, no subscriptions, no cloud dependencies.

**Q: Can I use this for multiple properties?**
Yes. Add as many properties as you need during setup, or add more later from Settings.

**Q: What about state-specific laws?**
The `compliance/` folder has jurisdiction-agnostic templates. The `examples/oregon/` folder shows how to fill them in for a specific state. Customize for yours.

**Q: Can I install it as a desktop app?**
Yes. When using Chrome, you'll see an "Install as app" prompt. The app works in its own window like a native application.

**Q: Can I use this without an AI assistant?**
Absolutely. It's a well-organized file system with a web dashboard that works on its own. The AI integration is a bonus.

**Q: What Python version do I need?**
Python 3.11 or later. The launcher checks this automatically and shows a clear message if you need to install it.

See [docs/faq.md](docs/faq.md) for more.

## License

MIT License — Copyright (c) 2026 Phil Johnston

See [LICENSE](LICENSE) for details.
